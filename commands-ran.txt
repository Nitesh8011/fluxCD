minikube start -p staging \
  --cpus=2 \
  --memory=4096 \
  --driver=docker

minikube start -p production \
  --cpus=2 \
  --memory=4096 \
  --driver=docker


export GITHUB_TOKEN=github_pat_****

flux bootstrap github \
  --owner=Nitesh8011 \
  --repository=fluxCD \
  --branch=master \
  --path=./clusters/staging \
  --personal


https://fluxcd.io/flux/cmd/flux_bootstrap_github/ - for other github bootstrap cmds

Staging:
flux bootstrap github \
  --owner=Nitesh8011 \
  --repository=fluxCD \
  --branch=master \
  --path=./clusters/staging \
  --context=staging \
  --personal


Production:
flux bootstrap github \
  --owner=Nitesh8011 \
  --repository=fluxCD \
  --branch=master \
  --path=./clusters/production \
  --context=production \
  --personal